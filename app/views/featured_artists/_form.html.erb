<%= form_for(@featured_artist) do |f| %>
  <% if @featured_artist.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@featured_artist.errors.count, "error") %> prohibited this featured_artist from being saved:</h2>

      <ul>
      <% @featured_artist.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :artist_id %><br>
    <%= f.text_field :artist_id %>
  </div>
  <div class="field">
    <%= f.label :song_id %><br>
    <%= f.text_field :song_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
