<%= form_for(@station_album) do |f| %>
  <% if @station_album.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@station_album.errors.count, "error") %> prohibited this station_album from being saved:</h2>

      <ul>
      <% @station_album.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :station_id %><br>
    <%= f.text_field :station_id %>
  </div>
  <div class="field">
    <%= f.label :album_id %><br>
    <%= f.text_field :album_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
